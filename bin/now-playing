#!/bin/bash
STATUS=$(playerctl -p spotify status 2> /dev/null)

if [ $? -ne 0 ]; then
	exit 1
fi

if [[ "$STATUS" != *"Playing"* ]]; then
	exit 1
fi

echo "$(playerctl metadata xesam:artist 2> /dev/null) - $(playerctl metadata xesam:title 2> /dev/null)"
